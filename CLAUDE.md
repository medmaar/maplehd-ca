@AGENTS.md

## Git Workflow

ALWAYS commit and push directly to main branch. NEVER create new branches. NEVER use git checkout -b. Always use:

```
git add . && git commit -m 'message' && git push origin main
```
